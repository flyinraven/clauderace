# Copy to .env.production before running `npm run build`.
#
# The frontend is served from SiteGround and the API lives on Render, so the
# built bundle needs the API's absolute URL baked in. No trailing slash.
VITE_API_BASE_URL=https://race-exam-api.onrender.com
